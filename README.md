# Historical Data Engine (minimal scaffold)

This scaffold provides a minimal DuckDB-backed historical data engine with a `PointInTimeQuery` enforcing cutoff semantics.

Try:

```bash
python -m pip install -r requirements.txt
pytest -q
```
# ckl